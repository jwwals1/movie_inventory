extends layout

block content
  h1= title
  if movie_list.length
    ul
      each movie in movie_list
        li
          a(href=movie.url) #{movie.title}
          //- |  (#{book.author.name})

  else
    p There are no movies.
